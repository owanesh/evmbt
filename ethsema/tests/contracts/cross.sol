pragma solidity ^0.4.23;


contract Lottery {
    uint a = 0;
    function rand() view public returns(uint256) {
        // revert();
        return 1234;
    }
    function tttest() public returns(uint256) {
        // revert();
        a = 1;
        return 5678;
    }
}
